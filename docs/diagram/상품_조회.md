# 상품 조회

상품 단건 조회와 목록 조회. **둘 다 인증이 필요 없는 공개 엔드포인트.**

## 1. 상품 단건 조회 — `GET /api/v1/products/{product_id}`

| 항목       | 값                                                  |
| ---------- | --------------------------------------------------- |
| 핸들러     | `app/product/router.py:39 get_product_by_id`        |
| 인증       | **불필요** (공개)                                   |
| 성공       | `200 OK` + `ProductResponse`                        |
| 실패       | `404 Not Found`                                     |

```mermaid
sequenceDiagram
    autonumber
    actor Client
    participant Router as Router<br/>GET /products/{id}
    participant Service as ProductService
    participant Repo as ProductRepository
    participant DB as SQLite

    Client->>Router: GET /api/v1/products/{product_id}
    Router->>Service: get_product(product_id)
    Service->>Repo: get_by_id(product_id)
    Repo->>DB: SELECT * FROM products WHERE id = ?
    DB-->>Repo: row | None

    alt 상품 없음
        Repo-->>Service: None
        Service-->>Router: NotFoundException
        Router-->>Client: 404 Not Found<br/>"Product with ID {id} not found"
    else 상품 존재
        Repo-->>Service: Product
        Service-->>Router: Product
        Router-->>Client: 200 OK<br/>ProductResponse
    end
```

### 핵심 포인트

- 응답에는 비활성 상품(`is_active=False`)도 포함된다. 비활성 상품 노출을 막으려면 서비스 계층에서 필터링 추가가 필요.

---

## 2. 상품 목록 조회 — `GET /api/v1/products/`

| 항목          | 값                                                                            |
| ------------- | ----------------------------------------------------------------------------- |
| 핸들러        | `app/product/router.py:90 list_products`                                      |
| 인증          | **불필요** (공개)                                                             |
| 쿼리 파라미터 | `skip`, `limit`, `category` (enum), `is_active` (bool)                        |
| 성공          | `200 OK` + `List[ProductResponse]`                                            |

```mermaid
sequenceDiagram
    autonumber
    actor Client
    participant Router as Router<br/>GET /products/
    participant Service as ProductService
    participant Repo as ProductRepository
    participant DB as SQLite

    Client->>Router: GET /api/v1/products/?<br/>skip=0&limit=100&category=...&is_active=...
    Router->>Service: list_products(skip, limit, category, is_active)
    Service->>Repo: list(skip, limit, category, is_active)

    Note over Repo: 동적 쿼리 구성<br/>category 지정 시 WHERE category = ?<br/>is_active 지정 시 WHERE is_active = ?

    Repo->>DB: SELECT * FROM products<br/>[WHERE ...]<br/>OFFSET ? LIMIT ?
    DB-->>Repo: rows
    Repo-->>Service: List[Product]
    Service-->>Router: List[Product]
    Router-->>Client: 200 OK<br/>List[ProductResponse]
```

### 핵심 포인트

- **필터는 옵셔널** 이다. `category` / `is_active` 미지정 시 해당 조건은 WHERE 절에 추가되지 않는다.
- `is_active is not None` 으로 체크 — `False` 도 명시적 필터로 인정 (즉 비활성 상품만 조회 가능).
- `category` 는 `ProductCategory` enum 으로 검증되어, 유효하지 않은 값은 FastAPI 단계에서 422 로 거절된다.
- 빈 결과는 빈 리스트 (`[]`) 로 정상 응답. 404 가 아님.
